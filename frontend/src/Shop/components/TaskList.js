// TaskList.tsx
import React from '// 在任务列表上方添加分类按钮
<div className="categories">
  <button onClick={() => filterTasks('健康')}>健康</button>
  <button onClick={() => filterTasks('教育')}>教育</button>
  <button onClick={() => filterTasks('创意')}>创意</button>
</div>';

const TaskList = () => {
  return (
    <div>
      {/* 任务列表内容 */}
    </div>
  );
};

export default TaskList;